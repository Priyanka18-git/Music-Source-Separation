# Music-Source-Separation
This project uses traditional digital signal processing (DSP) algorithms in MATLAB, such as stereo channel subtraction and frequency filtering, to separate vocal tracks from instrumental audio mixes. It provides a lightweight, efficient tool for creating high-quality karaoke tracks without relying on heavy machine learning models.
